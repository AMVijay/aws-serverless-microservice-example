{
  "name": "aws-serverless-microservice-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx rimraf dist lambda.zip && npx mkdirp dist && tsc && zip -rq lambda.zip dist node_modules"
  },
  "author": "Vijayaraaghavan Manoharan",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.431.0",
    "@aws-sdk/lib-dynamodb": "^3.431.0",
    "@types/aws-lambda": "^8.10.125",
    "aws-lambda": "^1.0.7",
    "mkdirp": "^3.0.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
